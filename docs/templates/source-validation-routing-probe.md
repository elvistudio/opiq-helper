# Source-validation routing probe

This temporary documentation-only artifact exists solely to exercise the `core_only` GitHub Actions path in the stacked path-aware CI pull request.

It is not production documentation and must not be merged.
