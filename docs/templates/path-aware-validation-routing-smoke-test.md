# Path-aware validation routing smoke test

This temporary document exists only to exercise the reduced `core_only` GitHub Actions route introduced by the stacked path-aware validation change.

It is not curriculum material, a source record, a lesson, a teacher pack, or a stable project document. The validation pull request that contains it is disposable and must not be merged.
